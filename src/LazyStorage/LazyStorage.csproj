<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Version>0.0.1.0</Version>
    
    <PackageVersion>0.0.1-local</PackageVersion>
    <Authors>David Eadie</Authors>
    <Description>Automates the create of a storage layer when creating prototypes</Description>
    <Copyright>David Eadie</Copyright>
    <PackageLicenseUrl>https://github.com/TheEadie/LazyStorage/blob/master/LICENSE</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/TheEadie/LazyStorage</PackageProjectUrl>
    <RepositoryUrl>https://github.com/TheEadie/LazyStorage</RepositoryUrl>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DebugType>full</DebugType>
    <DebugSymbols>true</DebugSymbols>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="10.0.3" />
  </ItemGroup>

</Project>
